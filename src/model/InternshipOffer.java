package model;

// internship offer
public class InternshipOffer extends Offer {

    public InternshipOffer(String companyName, String email, double salary, String description, boolean requiresFee) {
        super(companyName, email, salary, description, requiresFee);
    }
}
